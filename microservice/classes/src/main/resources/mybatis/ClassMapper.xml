<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huike.eaas.microservice.classes.mappers.ClassMapper">
    <select id="findById" resultType="com.huike.eaas.microservice.classes.domains.Clazz">
        select user_id as id, name as classname from user limit 1;
    </select>

</mapper>